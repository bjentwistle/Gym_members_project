{% extends "base.jinja" %}


{% block content %}
<div class = top-border>
    <div class="centered"><strong>CC Gym Admin</strong></div>
    <img src="{{ url_for('static', filename='feet_weight_unsplash.jpg') }}" alt = "feet and round weight" style='width:20%;'>
</div>

<div class = "session-container">
{% for session in sessions %}

<div class = "session">
<h2> Session: {{ session.name}} </h2>
<h3> Duration: {{ session.duration }} </h3>
<a href = "/sessions/{{ sessions.index(session) }}" > Show session details </a> <!--add a link to each book selected by index in orders /-->
<p> Premium: {{ session.premium_session }} </p>

{# 
    {% if book.loaned %}
    <p><i> Loaned </i></p>
    {% endif %} #}


    {# <form class = 'form-inline-delete' action='/books/delete/{{book.unique_id}}' method= 'post'>
        <input type = 'submit' value = 'Delete Book' style = "border:none">
    </form> #}

</div>

{% endfor %}
</div>

{% endblock %}
